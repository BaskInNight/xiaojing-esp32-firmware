menu "Drain Service"

    config DRAIN_SERVICE_TEST_HOOKS
        bool "Enable test hooks for drain service tests"
        default n
        help
            Compile drain_service.c with hook points for
            deterministic testing. Only enable in test builds.

endmenu
